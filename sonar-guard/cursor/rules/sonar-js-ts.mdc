---
description: 编写或审查 JS/TS/Vue/React 代码、检查前端的 Sonar 合规问题时使用。
alwaysApply: false
---

# JS / TS / Vue / React 高频 Sonar 规则

## BLOCKER / CRITICAL

| 规则 | 问题与修法 | 风险 |
|---|---|---|
| S1525 | `debugger` 语句 → 删 | 🟢 |
| S2068 | 硬编码凭证 → 环境变量/配置;前端里的密钥要评估是否本就不该出现在前端 | 🟡/🔴 |
| S3923 | if/else 所有分支相同 → 笔误,弄清意图再改 | 🔴 修的是逻辑 bug |
| S6544 | Promise 误用作条件/漏 await → 补 await/catch | 🔴 时序变化,常是真 bug |
| S2871 | `Array.sort()` 排数字无比较器(按字符串排)→ 补 `(a,b)=>a-b` | 🔴 结果会变但原结果是错的 |
| S4123 | 对非 Promise 用 await → 删或修返回 | 🟡 |
| S5852 | ReDoS 正则 → 重写并双向验证 | 🔴 |

## MAJOR

| 规则 | 问题与修法 | 风险 |
|---|---|---|
| S1440 | `==`/`!=` → `===`/`!==`;`x == null` 改写要兼顾 undefined | 🟢 类型确定 / 🟡 涉及 null |
| S3504 | `var` → `let`/`const` | 🟡 提升语义消失 |
| S3776 | 认知复杂度过高 → 提取函数、提前 return | 🟢 |
| S1854 | 无用赋值 → 删(确认无副作用表达式) | 🟢 |
| S6582 | 可用可选链 `?.` | 🟢;`a && a.b` 中 a 为 ''/0 时行为不同 🟡 |
| S6606 | `??` 替代 `||` 提供默认值 | 🟡 `||` 把 0/''/false 也当缺省 |
| S2486 | 捕获异常被忽略 → 至少 console.error/上报 | 🟢 加日志 / 🔴 改抛出 |
| S6478 | React: 组件内定义组件 → 提到模块层 | 🟡 state 保留行为变(通常是修复) |
| S6479 | React: 列表 key 用 index → 唯一 id | 🟡 复用行为变化 |
| S6481 | React: Context value 每次渲染新建 → useMemo | 🟢 |

## MINOR 常见

console.log 调试残留(S2228)、删注释代码(S125)、嵌套三元改 if/else(S3358)、变量遮蔽重命名(S1117)、TS 多余类型断言删除(S4325)——均 🟢。

## 特别注意

- Vue SFC:Sonar 只分析 `<script>`;`<template>` 的问题(v-for 缺 key 等)审查时一并人工看。
- React Hooks 依赖数组(S6440 等):补依赖可能引发副作用重复执行,属 🔴,逐个确认。
- `??` 替换 `||`、`?.` 替换 `&&` 是前端最容易"修出 bug"的两类,务必检查左侧值可能为 0/''/false 的场景。
