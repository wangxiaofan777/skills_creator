---
description: 评估 Sonar 问题修复风险(会不会把功能改坏)时使用;给任何修复建议前必读。
alwaysApply: false
---

# 修复风险评估

回答用户最关心的问题:**"按 Sonar 的建议改,会不会把功能改坏?"**

## 风险等级

| 等级 | 含义 | 处理 |
|---|---|---|
| 🟢 零/低 | 不改变运行时行为,或由编译器/类型系统保证 | 直接修 |
| 🟡 中 | 多数情况安全,特定边界条件下可能变化 | 确认测试覆盖,修后跑测试,写明变化点 |
| 🔴 高 | 修复本质上**改变程序行为**(往往正是规则目的——原行为是 bug) | 先说明行为变化,用户确认后再改;单独提交 |

## 典型分级

- **🟢**:删未用 import/局部变量/私有方法(被反射/DI/序列化用到的"看似未用"升 🟡);私有成员重命名(public API 重命名 🔴);魔法数字/重复字符串提常量;删注释代码;纯提取函数降复杂度(逐行搬运不顺手优化)。
- **🟡**:资源关闭改 try-with-resources/with/defer(异常路径关闭时机变);日志替换 System.out/print(stdout 有无外部消费方要问);`x == null` 改 `===`(会漏 undefined,应改 `x === null || x === undefined`);列表 key 由 index 改 id(复用行为变);依赖小版本升级(跨主版本 🔴)。
- **🔴**:空 catch/裸 except 改抛出(带病运行的路径开始失败);加空指针防御(崩溃变静默跳过,需确认期望);equals/hashCode 修正;并发类修复(加锁/volatile);SQL 参数化中的动态表名/排序列(需白名单);正则修改(必须双向验证);异常类型收窄;public API 签名变更;安全行为变更(恢复证书校验等,可能导致联调失败,需协调)。

## 每条修复建议按此模板给出

```
- 风险等级: 🟡 中
- 行为变化点: 异常发生时连接将被立即释放(原来泄漏到 GC)
- 改坏的可能场景: 若调用方在 catch 后仍使用该连接(已检查本文件无此用法)
- 验证方案: 运行 OrderServiceTest, 重点看异常分支用例
```

## 通用原则

1. 无测试覆盖的 🟡/🔴 修复,先建议补测试,或明确告知"修复后无法自动验证"。
2. 批量修复按风险分组、分批提交,好回滚好定位。
3. 存量老代码的 🔴 问题默认建议"标记 Won't Fix / 加豁免注释 + 排期重构",而非上线前夕大改(豁免需团队规范允许并写明理由)。
