---
description: 编写或审查 Go 代码、检查 Go 的 Sonar 合规问题时使用。
alwaysApply: false
---

# Go 高频 Sonar 规则

## 高严重级

| 规则 | 问题与修法 | 风险 |
|---|---|---|
| S1751 | 循环至多执行一次(无条件 return/break)→ 核对意图 | 🔴 多为逻辑 bug |
| S2068 | 硬编码凭证 → 环境变量/配置 | 🟡 |
| S3923/S1862/S1764 | if/else 分支相同 / 条件重复 / 二元运算两侧相同 → 笔误,确认意图 | 🔴 |
| S4830 | TLS `InsecureSkipVerify: true` → 恢复校验 | 🔴 自签证书场景需协调 |
| S2245 | math/rand 用于安全场景 → crypto/rand | 🟡 |
| S1313 | 硬编码 IP → 配置 | 🟡 |

## 错误处理(Go 重点)

| 问题 | 修法 | 风险 |
|---|---|---|
| 忽略 error(`_ = f()` 或丢弃) | 接住并处理/上抛/记日志 | 🔴 失败分支是新行为 |
| `if err != nil {}` 空块 | 补处理逻辑 | 🔴 |
| panic 用于普通错误流 | 改返回 error | 🔴 调用方需同步处理 |
| errors.New(fmt.Sprintf(...)) | fmt.Errorf,需要包装用 `%w` | 🟢;`%w` 后 errors.Is/As 行为增强 🟡 |

## 通用代码味道

认知复杂度过高提取函数(S3776)、重复字符串提常量(S1192)、删注释代码(S125)、可合并嵌套 if(S1066)——均 🟢;函数返回过多值封装 struct 🟡(调用方同步改)。

## 特别注意

- goroutine 泄漏与 channel 死锁 Sonar 检出有限,review 时主动看:每个 `go func` 是否有退出路径、channel 是否可能无人收发。
- defer 在循环内(资源延迟到函数结束才释放):提取函数包裹 → 🟡,释放时机提前。
- context 缺失/未传递:补 context 是 API 变更 → 🔴。
