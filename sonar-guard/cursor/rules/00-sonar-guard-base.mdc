---
description: Sonar 代码合规检查基础工作流:写完/改完代码后对照 SonarQube 规范审查、输出报告、按风险分级修复。
alwaysApply: true
---

# Sonar Guard · 基础规则

仓库根目录存在 `.sonarguard.json` 时生效:**任何代码编写/修改完成后,主动对照 Sonar 规范审查本次变更**,不要等用户提醒——"上线前 CI 扫描才暴露问题"的根因就是平时没人主动查。

## 工作流

1. 确定变更范围:本次会话新建/修改的文件;用户要求全面检查时用 `git diff --name-only` + `git status`。
2. 按变更文件的语言加载对应规则卡片(Java / Python / JS·TS / Go),给修复建议前先读"修复风险评估"卡片定级。
3. 逐文件对照规则审查,输出固定结构报告:

```markdown
## Sonar 合规检查报告(模式:离线)

### 本次变更引入的问题(N 个)
| # | 文件:行 | 规则 | 严重级 | 问题 | 修复风险 |

#### 问题 1:<标题>(<规则号>)
- 为什么是问题 / 怎么修(给具体代码)/ 会不会改出问题(风险等级+理由+验证建议)

### 建议
- 🟢 可直接修:…  🟡 修后需跑测试:…  🔴 需用户确认再动:…
```

## 修复执行原则

- 🟢 零/低风险:直接修,修后简述改了什么。
- 🟡 中风险:修前确认测试覆盖,修后跑相关测试,写明行为可能的变化点。
- 🔴 高风险:**先向用户说明行为将如何变化,确认后再改**;单独提交。
- 多个修复按风险分组提交,不混在一个大改动里;只做规则要求的最小变更,不"顺手优化"。

## 注意

- 本规则集是离线高频规则子集,不等价于完整 Sonar 扫描,最终以 CI 上的扫描为准。
- Sonar 服务器查询与 pre-commit 钩子由 Claude Code 版 sonar-guard(scripts/)提供;Cursor 下需要服务器存量 issue 时,请用户从 Sonar 页面导出或粘贴。
- token 等凭证永远不写入任何会提交的文件,也不在报告中回显。
