---
description: 资深工程师代码风格的基础规则:探测技术栈 + 与栈无关的通用原则。
alwaysApply: true
---

# 资深代码风格 · 基础规则

目标:代码像工作十年以上的人写的——**平、直、少跳转,抽象只在真正赚到时出现**。前端"组件/hook 过度拆分"与后端"反复套方法"是同一个病,用同一套原则治。

## 先探测,别假设栈
动手前读 `pom.xml`/`build.gradle`(后端)与 `package.json`/`tsconfig.json`(前端),认出实际用的框架与库;并抽样读几个既有源文件学本地约定(目录划分、命名、DI/响应/异常写法、组件粒度、状态放哪、数据怎么取)。**匹配既有约定优先于套用本规则的偏好。**

## 第一原则:抽象必须"配得上"自己的存在
方法/类/组件/hook/composable 存在,必须满足之一,否则**内联掉**:① 真被复用(2 处以上);② 给不直观的操作命名;③ 把确实复杂的逻辑降到可读。
- **三次法则**:同一段逻辑出现第 3 次再抽;2 次先复制。
- 错误抽象比重复贵;就近原则;不写"包装的包装"(方法体只是原样转发就删掉)。

## 不为平台已有能力引入依赖
- 后端:`List.of/Map.of`、`String.join`、`Optional`、`Stream`、`record` 已有——别为这些拉 Guava/Hutool。
- 前端:可选链 `?.`、`??`、`Array.includes/at/flatMap`、`structuredClone`、`Intl` 已有——别为这些拉 lodash/大型日期库。

## 派生而非同步(两端最高频的过度复杂)
能由已有数据算出的值就当场算,别另存一份再手动同步。前端用 `computed`/渲染时派生,**不要**用 `useEffect`/`watch` 去同步可派生的状态;后端少冗余字段。

## 其余
守卫语句代替深层嵌套;命名表达意图;注释解释"为什么"不解释"做什么";不留死代码(`console.log`/`printStackTrace`/没用的 import 一律删);一个方法/组件做一件逻辑完整的事。

## 路由(按探测结果,相关的库规则会按需加载)
- 后端:MyBatis Plus / Lombok / Guava / MapStruct / Hutool / Spring / 缓存 → 见 `backend/*` 规则。
- 前端:React / Vue / TypeScript / 状态管理 / 样式 / 数据请求 → 见 `frontend/*` 规则。

> 精神是**做减法**。犹豫"要不要再封一层"时,默认**不要**——直到它真的赚到第三次复用。
