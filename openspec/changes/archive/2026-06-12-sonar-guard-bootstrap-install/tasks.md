## 1. Bootstrap 脚本

- [x] 1.1 实现自包含 `install_bootstrap.py`、`uninstall_bootstrap.py`
- [x] 1.2 `install.py` 成功时写入 `packageRoot` 到用户 config
- [x] 1.3 `install_repo_cli` 复制 bootstrap 为 `.sonarguard/install.py` 与 `uninstall.py`

## 2. 文档

- [x] 2.1 README：metis 内安装三条路径 + PowerShell 单行示例 + 排障

## 3. 验证

- [x] 3.1 从 metis cwd 用 bootstrap 绝对路径 install 成功
